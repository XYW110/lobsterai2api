# lobsterai2api 部署配置 — 复制为 .env 后按需修改
#   cp .env.example .env

# ---------------------------------------------------------------------------
# 镜像
#   从 Docker Hub 拉取 -> 保持默认（已发布：linux/amd64 + linux/arm64）
#   本地构建           -> docker compose build 会以该名字打标
# ---------------------------------------------------------------------------
LB2A_IMAGE=dockercom110/lobsterai2api:latest

# ---------------------------------------------------------------------------
# 上游地址（必填，容器内无默认值）
# ---------------------------------------------------------------------------
LB2A_UPSTREAM_BASE=
# OAuth 登录门户地址（仅登录时需要）
LB2A_LOGIN_PORTAL=

# ---------------------------------------------------------------------------
# 本地 API Key —— 强烈建议设置，留空 = 任何人都能调用并消耗你的额度
# ---------------------------------------------------------------------------
LB2A_API_KEY=

# ---------------------------------------------------------------------------
# 监听 / 端口
# ---------------------------------------------------------------------------
LB2A_PORT=8367
# 0.0.0.0 = 对局域网/公网开放；127.0.0.1 = 仅本机
LB2A_BIND_HOST=0.0.0.0
# 登录回调端口（需与登录页校验的 127.0.0.1 回调地址一致）
LB2A_LOGIN_PORT=1455

# ---------------------------------------------------------------------------
# 时区（影响每日签到/保活触发时间点）
# ---------------------------------------------------------------------------
TZ=Asia/Shanghai

# ---------------------------------------------------------------------------
# 运行身份（可选）——Linux 下设为你的 id -u / id -g，避免挂载目录属主变成 root
#   留空 = 以 root 运行（兼容性最好）
# ---------------------------------------------------------------------------
# PUID=1000
# PGID=1000

# ---------------------------------------------------------------------------
# 账号池调优（通常不用改）
# ---------------------------------------------------------------------------
LB2A_TIMEOUT_SECONDS=180
LB2A_ERR_THRESHOLD=3
LB2A_HARD_CREDIT=12h
LB2A_SOFT_RATE=60s
LB2A_ERR_COOLDOWN=10m
